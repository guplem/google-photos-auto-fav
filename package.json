{
  "name": "google-photos-auto-fav",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Chrome extension that marks every photo of a Google Photos album as favourite when its file name is in a list you provide.",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc --noEmit -p jsconfig.json",
    "test": "node --test",
    "check": "npm run format:check && npm run typecheck && npm test"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/node": "^26.4.1",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2"
  }
}
